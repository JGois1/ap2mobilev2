<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ap2mobile"
        tools:targetApi="31">

        <!-- Activity Inicial -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activities do Questionário -->
        <activity android:name=".Question1Activity" android:parentActivityName=".MainActivity"/>
        <activity android:name=".Question2Activity" android:parentActivityName=".Question1Activity"/>
        <activity android:name=".Question3Activity" android:parentActivityName=".Question2Activity"/>
        <activity android:name=".Question4Activity" android:parentActivityName=".Question3Activity"/>

        <!-- Activity de Resultado -->
        <activity android:name=".ResultActivity" android:parentActivityName=".MainActivity"/>

    </application>

</manifest>